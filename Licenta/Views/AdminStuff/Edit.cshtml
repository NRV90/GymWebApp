@using Licenta.ViewModels
@model UserViewModel
@{


}
<h1>Here you will be able to modify users as many as you want too :}</h1>
<p>@Model.Id</p>
<p>@Model.Email</p>
<p>@Model.LastName</p>
<p>@Model.FirstName</p>
<p>@Model.Role</p>

  <form class="register-form" method="post" asp-controller="AdminStuff" asp-action="Modify" >


    @Html.HiddenFor(user => user.Id)
    <br />
            <input asp-for="LastName" type="text" placeholder="name" required id="nume" value="@Model.LastName" />
            <br />
            <input asp-for="FirstName" type="text" placeholder="prenume" required id="prenume" value="@Model.FirstName"  />
              <br />
            <input asp-for="Email" type="text" placeholder="email address" id="email" value="@Model.Email" required     />
              <br />
           Roles:
           <br />

    <p>User:  @Html.RadioButtonFor(user=>user.Role,"User")</p>
    <p>Admin: @Html.RadioButtonFor(user=>user.Role,"Admin")</p>
    <p>Owner: @Html.RadioButtonFor(user=>user.Role,"Owner")</p>
    <p>Trainer:   @Html.RadioButtonFor(user=>user.Role,"Trainer")</p>
    <button id="submitEdit" type="submit">Modify</button>
           
           
        </form>
  
